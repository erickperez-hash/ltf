name = "listing-truth-finder-api"
main = "src/index.js"
compatibility_date = "2024-11-22"

# Environment variables (secrets should be set via: wrangler secret put <NAME>)
[vars]
# Add non-sensitive variables here

# CORS configuration
[cors]
allowed_origins = ["*"]
allowed_methods = ["GET", "POST", "OPTIONS"]
allowed_headers = ["*"]
